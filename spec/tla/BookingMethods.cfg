\* TLC Configuration for BookingMethods.tla

CONSTANTS
    Currency = "AAPL"
    CostCurrency = "USD"
    MaxLots = 4
    MaxUnits = 20

INIT Init

NEXT Next

INVARIANTS
    Invariant
    TypeOK
    CostBasisTracked
    FIFOProperty
    LIFOProperty
    HIFOProperty
    AVERAGEProperty
    STRICTProperty
    STRICT_WITH_SIZEProperty

\* All 7 booking methods now have strong invariants verifying
\* correct lot selection based on the matching_lots snapshot
